class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int c=nums.size();
        c=(c*(c+1))/2;
        int s=0;
        for(int i=0;i<nums.size();i++)
        { s+=nums[i];
        }
        return c-s;
    }
};
