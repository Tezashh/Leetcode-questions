class Solution {
public:
    int reverse(int x) {
        long rever=0;
        int c;
        while(x){
         c=x%10;
        rever=rever*10+c;
        x=x/10;
        }
        
            if(rever>INT_MAX || rever<INT_MIN) return 0;
        
        
        return rever;
    }
    
};
